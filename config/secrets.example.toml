[ssh_passwords]
# 明文密码示例。把这个文件复制为 config/secrets.toml 后填写真实密码。
# key 可以写 SSH config 里的 Host、HostName，或者 user@HostName。
# "gpu-box" = "your-password"
# "192.0.2.10" = "your-password"
# "alice@192.0.2.10" = "your-password"
